# Database Configuration
# PostgreSQL connection string for Prisma
# Format: postgresql://user:password@host:port/database
#
# You can use 'postgres' as the hostname for both local development and Docker.
# The application automatically adjusts the hostname:
#   - In Docker: 'postgres' is used as-is (Docker service name)
#   - Local development: 'postgres' is automatically converted to 'localhost'
#
# Example with connection pool settings:
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/mymoney?connection_limit=100&pool_timeout=20&connect_timeout=10

# OIDC Authentication (Pocket ID)
# Required for authentication - server will not start without these
OPENID_DISCOVERY_URL=http://localhost:8080/.well-known/openid-configuration
OPENID_CLIENT_ID=your-client-id
OPENID_CLIENT_SECRET=your-client-secret

# Server Configuration
# Port for the GraphQL server (default: 4000)
PORT=4000

# Environment
# Set to 'development' or 'production'
NODE_ENV=development

# CORS Configuration (Optional)
# Comma-separated list of allowed origins for CORS
# Defaults to http://localhost:3000 and http://127.0.0.1:3000 if not set
CORS_ORIGIN=http://localhost:3000,http://127.0.0.1:3000

# Allowed Origins for CSRF Protection (Optional)
# Comma-separated list of origins allowed for mutations
# Defaults to empty array if not set
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Development Login (Optional)
# Enable development login with username/password
# These are only used when dev login is enabled via frontend env variable
DEV_USERNAME=devuser
DEV_PASSWORD=devpassword

# Dev Auth Secret (Optional)
# Secret key for signing JWT tokens for dev login
# If not set, a random secret will be generated (not recommended for production)
DEV_AUTH_SECRET=your-dev-auth-secret-key-here
