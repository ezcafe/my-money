# GraphQL API Configuration
# Backend GraphQL endpoint URL used by the browser.
# For Docker-based setups, the host + port here should match the externally exposed backend
# (BACKEND_URL/BACKEND_PORT defined in the root .env/.env.example).
# Defaults to http://localhost:4000/graphql if not set.
REACT_APP_GRAPHQL_URL=http://localhost:4000/graphql

# OIDC Authentication (Pocket ID) - Optional
# These are optional and can be used for client-side OIDC configuration
# If not set, the frontend will use default values or values from the backend
REACT_APP_OPENID_DISCOVERY_URL=http://localhost:8080/.well-known/openid-configuration
REACT_APP_OPENID_CLIENT_ID=your-client-id
REACT_APP_OPENID_CLIENT_SECRET=your-client-secret-here